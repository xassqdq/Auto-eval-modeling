# ============================================================
# AutoEval-Modeling 依赖清单
# Python >= 3.10
# ============================================================

# ---------- 数值计算与数据处理 ----------
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0
scikit-learn>=1.3.0
openpyxl>=3.1.0          # Excel 读写支持

# ---------- 优化 ----------
pulp>=2.7.0               # 线性规划（DEA）
cvxpy>=1.4.0              # 凸优化（博弈论组合赋权）

# ---------- 可视化 ----------
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.18.0
kaleido>=0.2.1            # plotly 静态图片导出

# ---------- 工作流引擎 ----------
networkx>=3.1             # DAG 构建与拓扑排序
pydantic>=2.5.0           # 配置数据校验
PyYAML>=6.0               # YAML 配置文件解析

# ---------- 模板引擎 ----------
Jinja2>=3.1.0             # LaTeX / Python 代码模板渲染

# ---------- Web 交互界面 ----------
streamlit>=1.30.0
gradio>=4.0.0

# ---------- 安全沙箱 ----------
RestrictedPython>=7.0     # 受限代码执行环境

# ---------- 自然语言处理（可选）----------
# openai>=1.0.0           # ChatGPT API（按需启用）
# transformers>=4.35.0    # 本地 LLM（按需启用）
jieba>=0.42.1             # 中文分词（关键词提取）

# ---------- 报告与文件处理 ----------
python-docx>=1.0.0        # Word 文档生成（可选）
Pillow>=10.0.0            # 图片处理

# ---------- 开发与测试 ----------
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0
flake8>=6.0.0
rich>=13.0.0              # 终端美化输出
tqdm>=4.66.0              # 进度条
tabulate>=0.9.0           # 终端表格输出